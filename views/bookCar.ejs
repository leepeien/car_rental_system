<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Book Car</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
  <%- include('navbar'); %>
  <div class="container mt-5">
    <div class="card p-4 shadow">
      <h2 class="mb-4 text-center">Book This Car</h2>

      <div class="row">
        <div class="col-md-5">
          <img src="/images/<%= car.image %>" class="img-fluid rounded" alt="Car Image">
        </div>
        <div class="col-md-7">
          <h4><strong><%= car.car_model %></strong></h4>
          <p><strong>Type:</strong> <%= car.car_type %></p>
          <p><strong>Rental Rate:</strong> $<%= parseFloat(car.rental_rate).toFixed(2) %> per day</p>
          <p><strong>Rental Term:</strong> <%= car.rental_term %></p>
          <p><strong>Availability:</strong> <%= car.availability === 1 ? "Yes" : "No" %></p>
          
          <form action="/add-to-rental/<%= car.carId %>" method="POST">
            <div class="mb-3">
              <label for="days">Number of Days:</label>
              <input type="number" name="days" id="days" min="1" value="1" class="form-control w-50" required>
            </div>
            <button type="submit" class="btn btn-success">Add to Cart</button>
            <a href="/cars" class="btn btn-outline-secondary ms-2">Back to Cars</a>
          </form>
        </div>
      </div>
    </div>
  </div>
</body>
</html>
